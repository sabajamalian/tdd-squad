{
  "name": "tdd-squad",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "TDD workflow encoded as a Squad agent team — Red (write failing tests) → Green (make tests pass) → Blue (refactor for quality).",
  "scripts": {
    "start": "npx tsx index.ts"
  },
  "keywords": ["squad", "tdd", "test-driven-development", "ai-agents", "red-green-refactor"],
  "dependencies": {
    "@bradygaster/squad-sdk": "^0.8.0"
  },
  "devDependencies": {
    "tsx": "^4.19.4",
    "typescript": "^5.7.0"
  }
}
